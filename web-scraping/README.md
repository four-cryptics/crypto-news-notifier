# Running web-scraping scripts

1. Run `npm install` or `yarn install` to install all of the dependencies.
2. Build the package, using `npm run-script build` or `yarn build`
3. Run the webscraper using `npm run-script scrape` or `yarn scrape`